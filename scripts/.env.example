# Copy this file to .env and fill in values before running sync-zones or scheduling it.
# Generate an API token in the Technitium admin UI: Settings > API Keys
DNS_API_TOKEN=your-api-token-here
DNS_API_URL=https://localhost:53443
